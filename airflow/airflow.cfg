[core]
# Point to the dags directory (relative to AIRFLOW_HOME which is the airflow/ directory)
dags_folder = dags
load_examples = False

[database]
# Use SQLite database in this directory (absolute path via AIRFLOW_HOME)
sql_alchemy_conn = sqlite:///${AIRFLOW_HOME}/airflow.db
